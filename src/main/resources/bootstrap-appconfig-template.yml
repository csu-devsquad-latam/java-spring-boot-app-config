spring:
  cloud:
    azure:
      appconfiguration:
        enabled: true
        stores:
          - endpoint: <set your app configuration url https://service-name.azconfig.io>
            selects:
              - key-filter: '<app filter>:'
                label-filter: '<app versions>'